import React from 'react';
import BudgetReportsPage from '@/features/budget/components/BudgetReportsPage';

/**
 * Reports Page
 * 
 * Wrapper component for the budget reports feature.
 * Provides budget analysis, spending breakdowns, and financial reports.
 */
const Reports: React.FC = () => {
  return <BudgetReportsPage />;
};

export default Reports;